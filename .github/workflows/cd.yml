name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy (default: this commit SHA)"
        required: false
        type: string

concurrency:
  group: cd-production
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository }}

jobs:
  prepare:
    name: Resolve image tag
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.tag }}
    steps:
      - name: Image tag
        id: meta
        run: |
          if [ -n "${{ inputs.image_tag }}" ]; then
            TAG="${{ inputs.image_tag }}"
          else
            TAG="${GITHUB_SHA}"
          fi
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "Using IMAGE_TAG=${TAG}"

  build-and-push:
    name: Build & push ${{ matrix.service }}
    needs: prepare
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: true
      matrix:
        include:
          - service: identity-service
            dockerfile: apps/identity-service/Dockerfile
            context: .
            build_args: ""
          - service: media-service
            dockerfile: apps/media-service/Dockerfile
            context: .
            build_args: ""
          - service: community-service
            dockerfile: apps/community-service/Dockerfile
            context: .
            build_args: ""
          - service: communication-service
            dockerfile: apps/communication-service/Dockerfile
            context: .
            build_args: ""
          - service: ai-service
            dockerfile: Dockerfile
            context: .
            build_args: SERVICE_PATH=apps/ai-service
          - service: docs-portal
            dockerfile: infra/docs-portal/Dockerfile
            context: infra/docs-portal
            build_args: ""
          - service: marketplace-service
            dockerfile: apps/marketplace-service/Dockerfile
            context: apps/marketplace-service
            build_args: ""
          - service: donation-service
            dockerfile: apps/donation-service/Dockerfile
            context: .
            build_args: ""

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}/${{ matrix.service }}:${{ needs.prepare.outputs.image_tag }}
            ${{ env.IMAGE_PREFIX }}/${{ matrix.service }}:latest
          build-args: ${{ matrix.build_args }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}

  deploy:
    name: Deploy to server
    needs: [prepare, build-and-push]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Check required secrets
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          GHCR_PULL_TOKEN: ${{ secrets.GHCR_PULL_TOKEN }}
        run: |
          missing=0
          for name in SSH_HOST SSH_USER SSH_PRIVATE_KEY GHCR_PULL_TOKEN; do
            if [ -z "${!name}" ]; then
              echo "ERROR: secret $name is empty or not set"
              missing=1
            else
              echo "OK: $name is set"
            fi
          done
          if [ "$missing" -ne 0 ]; then
            echo ""
            echo "Add secrets at: Settings → Secrets and variables → Actions"
            echo "If this job uses Environment 'production', also add them under"
            echo "Settings → Environments → production → Environment secrets"
            echo "(or remove the environment gate so repo secrets are used)."
            exit 1
          fi

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.2
        env:
          IMAGE_TAG: ${{ needs.prepare.outputs.image_tag }}
          IMAGE_REGISTRY: ghcr.io/${{ github.repository }}
          GHCR_USER: ${{ github.repository_owner }}
          GHCR_TOKEN: ${{ secrets.GHCR_PULL_TOKEN }}
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          command_timeout: 30m
          envs: IMAGE_TAG,IMAGE_REGISTRY,GHCR_USER,GHCR_TOKEN,DEPLOY_PATH
          script: |
            set -euo pipefail
            APP_DIR="${DEPLOY_PATH:-/opt/charity-platform}"
            cd "$APP_DIR"

            if [ ! -f .env ]; then
              echo "ERROR: $APP_DIR/.env is missing. Copy from .env.example and fill secrets first."
              exit 1
            fi

            # Update compose + infra config from repo
            # Drop local dirty files so pull cannot abort (server must track origin/main)
            git fetch origin main
            git checkout main
            git reset --hard origin/main
            git clean -fd -e .env -e .env.local -e '*.override.yml'

            # Pull private (or rate-limited) images from GHCR
            echo "$GHCR_TOKEN" | docker login ghcr.io -u "$GHCR_USER" --password-stdin

            export IMAGE_REGISTRY IMAGE_TAG
            chmod +x scripts/deploy.sh
            ./scripts/deploy.sh
